import {AppPath} from "../types/AppPaths";

export const appPaths: Record<string, AppPath> = {
  dashboard: {
    path: "/dashboard",
  },
  settings: {
    path: "/settings",
  },
  tracking: {
    path: "/tracking",
  },
};
